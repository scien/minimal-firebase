{
  "author": "Bryant Williams <b.n.williams@gmail.com>",
  "name": "firebase-sync",
  "description": "Use Firebase synchronously through the REST api",
  "devDependencies": {
    "coffee-script": "1.8.x",
    "expect.js": "*",
    "jsdom": "3.1.x",
    "mocha": "*",
    "uglify-js": "2.4.x"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/scien/firebase-sync.git"
  },
  "scripts": {
    "compile": "cake compile",
    "prepublish": "npm run compile",
    "pretest": "npm run compile",
    "test": "mocha"
  },
  "version": "1.0.0"
}
